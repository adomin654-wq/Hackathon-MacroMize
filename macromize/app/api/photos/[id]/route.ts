import {database} from '@/lib/guest-db';import {photos} from '@/lib/photos';import {guest} from '@/lib/guest';
export async function GET(request:Request,{params}:{params:Promise<{id:string}>}){try{const {id}=await params;const row=await database().prepare('SELECT object_key,content_type FROM guest_files WHERE id=? AND guest_id=?').bind(id,guest(request)).first<{object_key:string;content_type:string}>();if(!row)return new Response('Not found',{status:404});const object=await photos().get(row.object_key);if(!object)return new Response('Not found',{status:404});return new Response(object.body,{headers:{'Content-Type':row.content_type,'Cache-Control':'private, no-store','X-Content-Type-Options':'nosniff'}});}catch{return new Response('Photo unavailable',{status:503});}}
